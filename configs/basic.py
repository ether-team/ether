#!/usr/bin/env python -tt

"""Configuration for test consumer."""

from ether.configs.common import host, port, user, \
     password, vhost

CONFIG = {
    "host": host,
    "port": port,
    "user": user,
    "password": password,
    "vhost": vhost,
}
